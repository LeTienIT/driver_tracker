name: driver_tracker
description: "Ứng dụng quản lý ví trị tài xế đưa đón khách hàng"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

  flutter_secure_storage: ^9.2.4
  dio: ^5.8.0+1
  flutter_riverpod: ^2.6.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
  freezed: ^3.1.0
  json_serializable: ^6.9.5


flutter:
  uses-material-design: true
  assets:
    - assets/background_login.jpg
